firecrawl-py>=1.0.0
google-cloud-firestore>=2.14.0
google-genai>=1.0.0
pydantic>=2.0
pytest-asyncio>=0.21.0
pytest>=7.0.0
python-dotenv>=1.0.0
# [webhooks] extra kell a bot_service.py webhook módjához
# (Application.run_webhook -> Updater.start_webhook) - enélkül a webhook-alapú
# Cloud Run Service élesben "RuntimeError: To use `start_webhook`, PTB must be
# installed via `pip install \"python-telegram-bot[webhooks]\"`" hibával
# elszáll induláskor, még mielőtt a portra kezdene figyelni.
python-telegram-bot[webhooks]>=20.0
pyyaml>=6.0
